version: '3.8'
services:
  postgres:
    image: postgres:15-alpine
    container_name: cleaning_db
    environment:
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: c0102
      POSTGRES_DB: registro_lyd_DB
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data: